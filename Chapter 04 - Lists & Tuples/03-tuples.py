# !!! Tuples are unchangeble, we cannot update the values of a tuple

t = ()                # Emtey tuple
t1 = (1,)             # Tuple with single element needs a comma
t2 = (1, 2, 3, 4)     # Tuple with multiple elements
print(t2[3])

