from module import greet

# When we import a function from a module, all the code in that module also get's imported and when we run the current file the automatically imported code also get's executed that why we need to add condition in the module.  
# Check module.py for more Info