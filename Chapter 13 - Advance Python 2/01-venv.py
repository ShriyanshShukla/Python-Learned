# pip install virtualenv will create an virtual environment

# pip freeze will list all the packages installed in the current environment

# pip freeze > requirements.txt will crete a txt file, So a use can install all the packages at once in any desired environment